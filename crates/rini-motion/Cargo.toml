[package]
name = "rini-motion"
version = "0.1.0"
edition = "2024"
description = "rini's animation geometry: flight plans, easing, z-bands. No rendering, no macOS calls."
license = "Apache-2.0"
publish = false

[lib]
doctest = false

[dependencies]
objc2-core-foundation = { workspace = true }
rini-shared = { workspace = true }
