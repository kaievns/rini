[package]
name = "rini-ipc"
version = "0.1.0"
edition = "2024"
description = "rini's IPC context: the command, query and event language, the Mach server, subscriptions, CLI hooks, and the client."
license = "Apache-2.0"
publish = false

[lib]
doctest = false

[dependencies]
rini-core = { workspace = true }
rini-mach-sys = { workspace = true }
thiserror = { workspace = true }
rini-runloop = { workspace = true }
continue = { workspace = true }
crossbeam-channel = { workspace = true }
dashmap = { workspace = true }
nix = { workspace = true }
parking_lot = { workspace = true }
serde = { workspace = true, features = ["derive", "std"] }
serde_json = { workspace = true }
tokio = { workspace = true, features = ["sync"] }
tracing = { workspace = true }

[dev-dependencies]
ctrlc = "3.5.2"
